Once you have generated all of the pages, then get it to start creating all of the components for a Next.js app 

"If I want to turn this into a proper nextjs web app, what UI components should I create? (Don't generate the files yet, just think and plan for now, also just focus on the minimal amount of UI components for now)"

pnpm dlx shadcn@latest init -> this might be a mac command